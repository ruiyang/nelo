
desc 'provision localhost with gnu global, exuberant ctags, pygments, global-pygments. so emacs ggtags can do code navigation'
task :emacs_gnu_global do
  p `sudo ansible-playbook ./playbooks/gnu_global.yml -i local`
end
